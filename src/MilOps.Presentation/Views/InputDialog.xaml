<Window x:Class="MilOps.Presentation.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ورود مقدار" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="CanMinimize"
        Background="{DynamicResource SurfaceBrush}">
    <Border Style="{StaticResource Card}" Margin="16" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" x:Name="PromptText" TextWrapping="Wrap" Margin="0,0,0,8"/>
            <TextBox Grid.Row="1" x:Name="ValueBox" Padding="6,4"/>
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="تأیید" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" Padding="20,6" IsDefault="True" Click="Ok_Click"/>
                <Button Content="انصراف" Style="{StaticResource SecondaryButton}" Padding="20,6" IsCancel="True" Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
