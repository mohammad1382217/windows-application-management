<UserControl x:Class="MilOps.Presentation.Views.SoldiersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="SoldiersView_Loaded">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Search:" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" Width="220" Padding="6,4" Margin="0,0,8,0"/>
            <Button Content="Search" Style="{StaticResource PrimaryButton}" Command="{Binding LoadCommand}" Margin="0,0,8,0"/>
            <Button Content="+ Add" Style="{StaticResource PrimaryButton}" Command="{Binding AddCommand}" Margin="0,0,8,0"/>
            <Button Content="Edit" Style="{StaticResource PrimaryButton}" Command="{Binding EditCommand}" Margin="0,0,8,0"/>
            <Button Content="Delete" Style="{StaticResource DangerButton}" Command="{Binding DeleteCommand}" Margin="0,0,8,0"/>
            <Button Content="🖨 Print" Style="{StaticResource PrimaryButton}" Command="{Binding PrintCommand}"/>
        </StackPanel>

        <!-- Filter -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Health:" Style="{StaticResource FieldLabel}"/>
            <ComboBox ItemsSource="{Binding HealthTypes}" SelectedItem="{Binding HealthFilter}" Width="140" Padding="4"/>
        </StackPanel>

        <!-- Grid -->
        <Border Grid.Row="2" Background="White" BorderBrush="#FFE0E0E0" BorderThickness="1">
            <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding Selected}"
                      AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal" CanUserAddRows="False" CanUserDeleteRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Code" Binding="{Binding PersonnelCode}" Width="100"/>
                    <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="120"/>
                    <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="140"/>
                    <DataGridTextColumn Header="Rank" Binding="{Binding Rank}" Width="100"/>
                    <DataGridTextColumn Header="National Code" Binding="{Binding NationalCode}" Width="120"/>
                    <DataGridTextColumn Header="Department" Binding="{Binding DepartmentName}" Width="120"/>
                    <DataGridTextColumn Header="Health" Binding="{Binding HealthType}" Width="90"/>
                    <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="60"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="{DynamicResource DangerBrush}" Margin="0,8,0,0"/>
    </Grid>
</UserControl>
